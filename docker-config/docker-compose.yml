version: '3.8'

services:
  # 后端服务
  backend:
    build: ..
    container_name: deepseek-backend
    restart: unless-stopped
    env_file:
      - ../.env
    networks:
      - chat-network
    healthcheck:
      test: ["CMD", "curl", "-X", "POST", "-H", "Content-Type: application/json", "-d", '{"message":"ping"}', "http://localhost:8000/chat"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: deepseek-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ../qian_duan:/usr/share/nginx/html:ro
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - chat-network

networks:
  chat-network:
    driver: bridge
